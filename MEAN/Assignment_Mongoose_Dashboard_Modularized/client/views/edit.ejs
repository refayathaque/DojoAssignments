<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit Rabbit's Profile</title>
  </head>
  <style media="screen">
    body {
      font-family: sans-serif;
      text-align: center;
    }
  </style>
  <body>
  <h2>Edit Rabbit's Profile!</h2>
  <hr>

  <% if(typeof(errors) != 'undefined') { %>
     <% for (var x in errors) { %>
      <h3><%= errors[x].message %></h3>
    <% } %>
  <% } %>

  <% for (var i = 0; i < rabbits.length; i++) { %>
  <form class="" action="/rabbits/<%= rabbits[i].id %>" method="post">
    <h3>Name</h3>
    <input type="text" name="name" value="">
    <h3>Age</h3>
    <input type="text" name="age" value="">
    <p>----</p>
    <input type="submit" name="submit" value="Save my new information!">
    <hr>
  </form>

  <form class="" action="/rabbits/destroy/<%= rabbits[i].id %>" method="post">
    <input type="submit" name="submit" value="Pass me on to another adopter :(">
  </form>
  <% } %>

  </body>
</html>
