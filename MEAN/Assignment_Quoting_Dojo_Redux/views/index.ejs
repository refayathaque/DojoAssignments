<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>QuotingDojo</title>
  </head>
  <style media="screen">
    body {
      font-family: sans-serif;
      text-align: center;
    }
  </style>
  <body>
  <h2>Welcome to QuotingDojo!</h2>
  <hr>

  <% if(typeof(errors) != 'undefined') { %>
     <% for (var x in errors) { %>
      <h3><%= errors[x].message %></h3>
    <% } %>
  <% } %>

    <form class="" action="/add_quote" method="post">
      <h3>Name</h3>
      <input type="text" name="name" value="">
      <h3>Quote</h3>
      <textarea type="text" name="quote" rows="10" cols="50"></textarea>
      <p>----</p>
      <input type="submit" name="submit" value="Add my quote!">
      <hr>
    </form>

    <a href="/skip_to_quotes"><button type="button">Skip to quotes!</button></a>

  </body>
</html>
